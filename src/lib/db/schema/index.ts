// ---------------------------------------------------------------------------
// Database Schema — ReadMeet Insight
// ---------------------------------------------------------------------------
// All table definitions are exported from here.
// Drizzle Kit reads this file to generate migrations.
//
// Tables to be implemented (per dev.md §6):
//   books, book_chunks, workflow_runs, workflow_steps,
//   book_analysis, embeddings, quotes, themes, user_library
//
// The schema is built incrementally — each new feature adds its
// table definitions in this directory and re-exports them here.
// ---------------------------------------------------------------------------

// Placeholder: schema tables will be defined in subsequent steps.
// Step 4 (State Design) will create the full Drizzle schema.

export {};
